// main.js - Mount React app to the DOM

// Create a React root using the DOM element with id 'root'
const root = ReactDOM.createRoot(document.getElementById('root'));

// Render the main App component inside the root
root.render(<App />);